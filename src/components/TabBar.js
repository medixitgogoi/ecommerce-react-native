import { StyleSheet, Text, View } from 'react-native'

const TabBar = () => {
    return (
        <View style={{ backgroundColor: "green", height: 70, }}>
            <Text style={{ color: "#000" }}>Bottom</Text>
        </View>
    )
}

export default TabBar

const styles = StyleSheet.create({})